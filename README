Installation:
npm install
npm start

then open a web browser and navigate to
http://localhost:8000/api/arrangements

this endpoint provides all arrangements in the database (should only be 1 currently)
